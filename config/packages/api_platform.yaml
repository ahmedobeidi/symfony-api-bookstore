api_platform:
    title: bookstore
    version: 1.0.0
    swagger:
        api_keys:
            JWT:
                name: Authorization
                type: header    
    formats:
        jsonld: ['application/ld+json']
    defaults:
        stateless: true
        cache_headers:
            vary: ['Content-Type', 'Authorization', 'Origin']